Let's set up the project. First, create a new virtual environment with python -m venv .venv. Second, activate it. Third, install the dependencies with pip install -e dot. Fourth, copy the example env file to dot env and fill in your key. Fifth, run the tests to confirm everything works. The order matters here: if you install before creating the venv, the packages land in your global site-packages, which you don't want.
