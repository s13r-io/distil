# TRACKER — Distil

Shared source of truth for owner (human) and agent (AI). **The agent updates this file as it
works**; the owner reviews it to see progress and to answer anything in "Decisions needed".

**Status values:** `todo` · `in_progress` · `blocked` · `review` · `done`
**Workflow per task:** test written (red) → implemented (green) → tests passing → `review` →
owner/agent marks `done`. Fill `Notes` with one line (commit hash, blocker, question).

Legend: T? = tests written · C? = code done · P? = tests passing · ✅/⬜

---

## Phase 0 — Scaffold & CI
| ID  | Task                                  | Status | T? | C? | P? | Owner | Notes |
|-----|---------------------------------------|--------|----|----|----|-------|-------|
| 0.1 | Repo, pyproject, package, gitignore   | done   | ✅ | ✅ | ✅ | agent | pyproject (py>=3.11), distil/ pkg, tests/{unit,eval,fixtures}, .gitignore (kb/,data/,.env) |
| 0.2 | LLMClient + FakeClient + Anthropic    | done   | ✅ | ✅ | ✅ | agent | Protocol + FakeClient (records calls for T-Q2) + AnthropicClient skeleton (env-driven, lazy key) |
| 0.3 | pytest harness + markers              | done   | ✅ | ✅ | ✅ | agent | unit/eval markers; conftest auto-skips eval w/o ANTHROPIC_API_KEY; smoke test green |
| 0.4 | GitHub Actions CI green               | done   | ✅ | ✅ | ✅ | agent | .github/workflows/ci.yml: ruff + pytest tests/unit on push/PR; eval job gated |

## Phase 1 — Data layer
| ID  | Task                       | Status | T? | C? | P? | Owner | Notes |
|-----|----------------------------|--------|----|----|----|-------|-------|
| 1.1 | Pydantic models (T-M1..M4) | done   | ✅ | ✅ | ✅ | agent | Profile+KBEntry+nested; closed enums (extra=forbid); quote mandatory, ts/locator optional; round-trip lossless. 13 tests |
| 1.2 | store.py SQLite+md (T-S*)  | done   | ✅ | ✅ | ✅ | agent | md front-matter=full JSON (lossless)+readable body; SQLite entries+profiles; upsert; candidate lookup; persists across instances. 8 tests |
| 1.3 | ingest.py .srt/.txt/.md/paste, ts optional (T-I*) | done | ✅ | ✅ | ✅ | agent | stage 0, pure; SRT+inline+paragraph parsers; uniform Segment{text,timestamp?,locator}; IngestError on empty/binary/missing. 9 tests |

## Phase 2 — Profile update logic (pure)
| ID  | Task                                | Status | T? | C? | P? | Owner | Notes |
|-----|-------------------------------------|--------|----|----|----|-------|-------|
| 2.1 | profile_update.py (T-P1..P8)        | done   | ✅ | ✅ | ✅ | agent | pure EMA; all 7 SCHEMA §3 rows; bad_source no-op on user; bounded weights; monotone confidence. 11 tests |

## Phase 3 — Triage
| ID  | Task                              | Status | T? | C? | P? | Owner | Notes |
|-----|-----------------------------------|--------|----|----|----|-------|-------|
| 3.1 | Triage prompt template            | done   | ✅ | ✅ | ✅ | agent | prompts/triage.py v1; honest verdict instruction; deictic evidence capture |
| 3.2 | Parse + TriageResult (T-T1,T-T2)  | done   | ✅ | ✅ | ✅ | agent | fence-tolerant JSON parse → Triage; ParseError on malformed/partial/bad-enum. 7 tests |
| 3.3 | little_to_extract short-circuit   | done   | ✅ | ✅ | ✅ | agent | is_low_value() signal; pipeline honors it in Phase 8 (T-PL2) — **superseded** 2026-08-09: short-circuit removed, `is_low_value()` deleted; see the changelog entry below and AGENTS.md |
| 3.4 | Eval tests (T-T4,T-T5)            | review | ✅ | ✅ | ⬜ | agent | written+gated; fixtures added (vlog/screen_share/rich/proc/mixed). NOT yet run — needs ANTHROPIC_API_KEY (owner) |

## Phase 4 — Extraction
| ID  | Task                               | Status | T? | C? | P? | Owner | Notes |
|-----|------------------------------------|--------|----|----|----|-------|-------|
| 4.1 | Type-routed extractors (T-E1,T-E2) | done   | ✅ | ✅ | ✅ | agent | routes by dominant triage type; type-specific fields; JSON-array parse→KnowledgeItem; ids assigned. 8 tests |
| 4.2 | Quote discipline <15 words (T-E4)  | done   | ✅ | ✅ | ✅ | agent | QuoteDisciplineError enforced in code (not model-dependent) |
| 4.3 | Faithfulness eval (T-E3)           | review | ✅ | ✅ | ⬜ | agent | HEADLINE. Deterministic faithfulness.quote_in_transcript() gate (6 unit tests). Eval T-E3 written+gated over 5 fixtures (ts+no-ts), UNRUN — needs API key (owner) |

## Phase 5 — Normalize
| ID  | Task                          | Status | T? | C? | P? | Owner | Notes |
|-----|-------------------------------|--------|----|----|----|-------|-------|
| 5.1 | Dedup / drop / stance (T-N*)  | done   | ✅ | ✅ | ✅ | agent | pure; drops unverifiable provenance (T-N2), backfills locator/ts (T-N4), merges near-dups (T-N1), stance untouched (T-N3). 7 tests |

## Phase 6 — Link to profile
| ID  | Task                          | Status | T? | C? | P? | Owner | Notes |
|-----|-------------------------------|--------|----|----|----|-------|-------|
| 6.1 | Goal-tied links (T-L1)        | done   | ✅ | ✅ | ✅ | agent | links with unknown linked_goal_id dropped; ids validated against profile goals+focus |
| 6.2 | Novelty reservation (T-L2)    | done   | ✅ | ✅ | ✅ | agent | deterministic ~ratio reservation; zero-ratio clears flags. 6 tests |
| 6.3 | Cold-start behavior (T-L3)    | done   | ✅ | ✅ | ✅ | agent | confidence<0.25 → prompt shows only stable goals+active focus, no affinities |

## Phase 7 — Graph linking (v0.1)
| ID  | Task                            | Status | T? | C? | P? | Owner | Notes |
|-----|---------------------------------|--------|----|----|----|-------|-------|
| 7.1 | Candidate lookup (T-G1)         | done   | ✅ | ✅ | ✅ | agent | deterministic topic-overlap lookup (type alone too broad); no candidates → no LLM call |
| 7.2 | Relation classify (T-G2)        | done   | ✅ | ✅ | ✅ | agent | LLM labels each candidate; only enum relations kept; 'none'/invalid dropped; capped at 3 candidates (LLM budget). 5 tests |

## Phase 7.5 — Note v1 reader-facing synthesis
| ID   | Task                                      | Status | T? | C? | P? | Owner | Notes |
|------|-------------------------------------------|--------|----|----|----|-------|-------|
| 7.5.1 | DistilledNote schema + note.py (T-DN*)    | done   | ✅ | ✅ | ✅ | agent | grounded teaching-note schema; cite-validating parser; topic normalization; deterministic fallback. 5 note tests |
| 7.5.2 | Pipeline/render/query/web integration     | done   | ✅ | ✅ | ✅ | agent | pipeline adds bounded note call; markdown/web show note before evidence; query vectors/prompts include cited note context; legacy entries load |

## Phase 8 — Pipeline
| ID  | Task                          | Status | T? | C? | P? | Owner | Notes |
|-----|-------------------------------|--------|----|----|----|-------|-------|
| 8.1 | Orchestrate 1→7 (T-PL1,T-PL3) | done   | ✅ | ✅ | ✅ | agent | ingest→triage→[short-circuit]→extract→normalize→link→note→graph→file; low-value files minimal w/ 1 LLM call; useful path budget≤4 w/o graph; note topics feed tags. 4 tests — **superseded** 2026-08-09: no short-circuit, triage+extract merged into one call; see the changelog entry below and ARCHITECTURE.md §2 |

## Phase 9 — CLI
| ID  | Task                        | Status | T? | C? | P? | Owner | Notes |
|-----|-----------------------------|--------|----|----|----|-------|-------|
| 9.1 | `distil run` (T-C1)         | done   | ✅ | ✅ | ✅ | agent | file/--paste/stdin → pipeline; prints entry path; --no-graph flag |
| 9.2 | `distil score` (T-C2)       | done   | ✅ | ✅ | ✅ | agent | persists feedback on entry + applies profile update (EMA alpha from env) |
| 9.3 | list/show + errors (T-C3)   | done   | ✅ | ✅ | ✅ | agent | list/show; friendly errors for missing key, bad file, unknown entry (no tracebacks). 7 tests |

## Phase 10 — Querying the knowledge base / read layer
| ID   | Task                                             | Status | T? | C? | P? | Owner | Notes |
|------|--------------------------------------------------|--------|----|----|----|-------|-------|
| 10.1 | Embedder protocol + Fake + real (T-X3)           | done   | ✅ | ✅ | ✅ | agent | Embedder protocol; FakeEmbedder (hashed BoW, overlap→similarity); LocalEmbedder/ApiEmbedder lazy; make_embedder(). 6 tests |
| 10.2 | sqlite-vec store; embed items at file (T-X1)     | done   | ✅ | ✅ | ✅ | agent | sqlite-vec 0.1.6 loads in env; vectors stored as JSON in item_vectors_meta (portable both backends); embed at file w/ embedder. 7 tests |
| 10.3 | `distil reindex` backfill (T-X2,T-C5)            | done   | ✅ | ✅ | ✅ | agent | store.reindex() idempotent + re-embeds on model change; CLI `distil reindex` (T-C5). pipeline embeds at file stage |
| 10.4 | Retrieval + ranking (T-Q1)                       | done   | ✅ | ✅ | ✅ | agent | retrieve(): cosine × feedback_mult × recency_mult; sorted desc |
| 10.5 | Relevance gate / abstention (T-Q2)               | done   | ✅ | ✅ | ✅ | agent | HEADLINE. gate before any synthesis; below threshold → abstain w/ ZERO LLM calls (asserted) |
| 10.6 | Grounded synthesis + sources + conflict (T-Q3..Q6)| done  | ✅ | ✅ | ✅ | agent | answer cites only retrieved items; ungrounded cites stripped+reported; resolvable sources; lookup-only; contradicts-edge conflict surfaced |
| 10.7 | `distil ask` + query eval (T-C4,T-Q7)            | review | ✅ | ✅ | ⬜ | agent | CLI ask (answer/abstain/lookup) + reindex done (4 unit tests + e2e smoke). Eval T-Q7 written+gated, UNRUN — needs API key + local embedder (owner) |

## Phase 11 — Packaging & deploy (local + Railway)
| ID   | Task                                       | Status | T? | C? | P? | Owner | Notes |
|------|--------------------------------------------|--------|----|----|----|-------|-------|
| 11.1 | Dockerfile + compose; bind 0.0.0.0:$PORT (T-A4) | done | ✅ | ✅ | ✅ | agent | python:3.11-slim; model baked at build; CMD binds 0.0.0.0:${PORT:-8000}; compose bind-mounts data/+kb/. T-A4 unit-tested |
| 11.2 | Auth gate; fail closed if public (T-A1..A3)| done   | ✅ | ✅ | ✅ | agent | web/auth.py + middleware; 9 tests (T-A1..A4) green |
| 11.3 | Volume-backed storage (/data)              | done   | ✅ | ✅ | ✅ | agent | DISTIL_DB_PATH/KB_DIR → /data in image+compose; runtime-mount caveat documented |
| 11.4 | railway.toml + DEPLOY_RAILWAY.md           | done   | ✅ | ✅ | ✅ | agent | present from kickoff; verified consistent w/ web.app:app + /health + auth |
| 11.5 | Git-remote backup of kb/                    | done   | ✅ | ✅ | ✅ | agent | scripts/backup_kb.sh: idempotent commit+push of kb/ to a SEPARATE private remote |
| 11.6 | README quickstart verified (incl. ask)     | done   | ✅ | ✅ | ✅ | both  | all 6 CLI commands (run/score/list/show/ask/reindex) match README; --help verified |
| 11.7 | Tag v0.0.1 + release workflow              | review | ✅ | ✅ | ⬜ | agent | release.yml (test-gated build+release on v* tag). Tag push deferred to owner (no push from build env) |

## Phase 12 — Web UI (v0.2)
| ID   | Task                                  | Status | T? | C? | P? | Owner | Notes |
|------|---------------------------------------|--------|----|----|----|-------|-------|
| 12.1 | FastAPI list/view/score + ask box; auth middleware (T-A2) | done | ✅ | ✅ | ✅ | agent | web/app.py: index w/ ask box, /entries, /entries/{id}, score, /ask; auth middleware front of data routes. 9 web tests |

## Phase 13 — YouTube video/playlist ingest (OKF Phase 1) (T-Y*)

| ID   | Task                                          | Status | T? | C? | P? | Owner | Notes |
|------|------------------------------------------------|--------|----|----|----|-------|-------|
| 13.1 | youtube.py fetch layer + web ADD-by-URL wiring | done   | ✅ | ✅ | ✅ | agent | distil/youtube.py (yt-dlp via injectable `run`): playlist enumeration + caption fetch → `ingest.ingest_srt_text`; web/app.py ADD input accepts a video/playlist URL, enqueues one `youtube` job per video through the existing web/jobs.py Worker; per-video failures skipped+reported, never fatal to the batch. `youtube` optional extra added. |

## Phase 14 — Per-video OKF export layer (OKF Phase 2) (T-OKF*, T-OKFL*)

| ID   | Task                                          | Status | T? | C? | P? | Owner | Notes |
|------|------------------------------------------------|--------|----|----|----|-------|-------|
| 14.1 | okf.py export layer (sources/+raw/+indexes)   | done   | ✅ | ✅ | ✅ | agent | Alongside lossless `kb/<id>.md`, `okf.py` derives a neutral OKF v0.1 bundle (`okf/sources/<slug>.md` + `okf/raw/<slug>.md` + regenerated indexes) with no feedback/application-link data; stable title-derived slug (falls back to `entry_id`, disambiguated on title collision). `store.file_entry(..., transcript=...)` triggers export at the File stage (pipeline.py passes the in-memory Transcript through); omitted for feedback-only re-files. `delete_entry` cascades to remove OKF pages. ARCHITECTURE.md §3/§4 updated. |
| 14.2 | okf_lint.py bundle validator                 | done   | ✅ | ✅ | ✅ | agent | stdlib-only, `python -m distil.okf_lint <okf_root>`; E1 frontmatter `type` present, E2 relative links resolve, E3 sources index completeness, E4 sources/raw parity (both directions, both errors). Concept checks added in 15.3 (E5-E8); entity orphan checks remain deferred (not implemented until that layer exists). |

## Phase 15 — Concept canonicalization (OKF Phase 3)

| ID   | Task                                          | Status | T? | C? | P? | Owner | Notes |
|------|------------------------------------------------|--------|----|----|----|-------|-------|
| 15.1 | canonicalize.py matching engine + concepts table (T-CANON1-7,9) | done | ✅ | ✅ | ✅ | agent | Per filed video, `canonicalize_entry(entry, store, client)` decides match/new/reject per knowledge item against a `concepts` SQLite table (`Store.find_concept_candidates`: cosine-similarity vs. concept centroids + token-overlap backstop, capped at `MAX_CONCEPT_CANDIDATES`; one batched LLM call per video; graph.py/note.py validation discipline). Idempotent (`retract_entry_concept_memberships` runs before every re-canonicalize and on `delete_entry`). Engine + data model only — no concept pages, OKF export, or pipeline wiring until 15.2/15.3 (T-CANON8/synthesis-capping also deferred, since nothing exists yet to cap). |
| 15.2 | synthesize_concept.py + OKF concept pages (T-SYN1-4, T-OKFC1-4, T-CANON8) | done | ✅ | ✅ | ✅ | agent | `synthesize_concept(concept, store, client)` reuses note.py's grounded-claim discipline at concept granularity (any claim with an unresolved `item_id` is dropped whole; deterministic one-liner fallback). Citations are code-rendered, never model-authored (`synthesize_concept.render_claim`). `okf.export_concept`/`remove_concept` write/retire `okf/concepts/<concept_id>.md` (frontmatter + rendered claims + "## Sources" appendix) and regenerate concepts/root indexes; `okf.render_source_with_concepts` is a separate post-canonicalize step adding each source page's "## Concepts covered" backlink, since File (Stage 7) runs before canonicalize (Stage 8). `canonicalize.synthesize_touched_concepts` caps synthesis at `MAX_CONCEPTS_TO_SYNTHESIZE_PER_VIDEO` (env `DISTIL_CONCEPTS_SYNTH_PER_VIDEO`, default 5), marking the rest `pending_synthesis=True`. Pipeline wiring, `enable_canonicalize` flag, and lint E5-E8 checks landed in 15.3. 281 unit tests green. |
| 15.3 | Wire canonicalize into pipeline + lint E5-E8 + validation gate (T-PL5,6, T-OKFL5-8, T-CANON-EVAL1,2) | done | ✅ | ✅ | ✅ | agent | `canonicalize.run_canonicalize_stage(entry, store, client)` is the one orchestration entry point (calls `canonicalize_entry` → `synthesize_touched_concepts` → `okf.export_concept`/`remove_concept` for touched/lost-membership concepts → `okf.render_source_with_concepts`); wired into `pipeline.run_pipeline` as Stage 8, gated by `PipelineConfig.enable_canonicalize` (default `True`). `okf_lint.py` gained E5 (concept frontmatter+index coverage), E6 (concept<->source citation integrity), E7 (bidirectional concept<->source links), E8 (no orphan concept pages). §6 validation gate (`pytest -m eval tests/eval/test_canonicalize_eval.py`) run 3/3 against the real model + a real local embedder with `CONCEPT_SIM_FLOOR=0.55`/`MAX_CONCEPT_CANDIDATES=5` unchanged; gate-produced bundle lints clean — default stays `True` per the honest-risk rule. 287 unit tests green, ruff clean. |

## Phase 16 — Concept↔concept typed edges (OKF Phase 3b)

| ID   | Task                                          | Status | T? | C? | P? | Owner | Notes |
|------|------------------------------------------------|--------|----|----|----|-------|-------|
| 16.1 | concept_graph.py typed edges + contradiction flag + render/lint | done | ✅ | ✅ | ✅ | agent | `concept_graph.link_concept_graph(concept, store, client)` applies `graph.py`'s exact deterministic-candidates-then-capped-LLM-classify shape one more time at concept granularity: `Store.find_concept_edge_candidates` (centroid-to-centroid cosine similarity, reusing `CONCEPT_SIM_FLOOR`, capped at `MAX_CONCEPT_EDGE_CANDIDATES=3`) feeds a per-candidate LLM call classifying one relation from `{contrasts_with, builds_on, related}` (new `Concept.edges: list[ConceptEdge]`; out-of-enum/`none` dropped, never trusted). `run_concept_edges_stage(touched, store, client)` is the Stage-9 orchestration entry point (wired into `pipeline.run_pipeline` right after Stage 8, gated by `PipelineConfig.enable_concept_edges`, default `True`): recomputes edges only for concepts actually synthesized this run (still-`pending_synthesis` concepts wait for their eventual synthesis pass), then `Store.prune_dangling_concept_edges()` drops any edge pointing at a concept deleted since, re-exporting every OKF page whose edges changed. `okf._render_concept` renders `## Contrasts with`/`## Builds on`/`## Related` (same-directory links) and, deterministically (no LLM), a `> **Contradiction:**` flag under a new `## Claims` heading whenever `synthesize_concept.find_claim_contradictions` finds a claim whose cited members' `KnowledgeItem.stance` values disagree. `okf_lint.py`'s E8 orphan check extended (not duplicated) to also recognize a concept's typed-edge links as valid inbound links for another concept — E2's generic link-resolution check already covered broken same-directory links for free. 18 new unit tests (`test_concept_graph.py` + additions to `test_synthesize_concept.py`/`test_okf.py`/`test_okf_lint.py`/`test_pipeline.py`); 305 unit tests green, ruff clean. Out of scope (per brief): query-over-concepts (Phase 18), concept maintenance CLI merge/split/sync-pending (Phase 17). |

## Phase 18 — Query-over-concepts (OKF Phase 3d)

| ID   | Task                                          | Status | T? | C? | P? | Owner | Notes |
|------|------------------------------------------------|--------|----|----|----|-------|-------|
| 18.1 | `retrieve_concepts` + wire into `ask`/`stream_ask` (T-Q9-13) | done | ✅ | ✅ | ✅ | agent | `query.retrieve_concepts(question, store, embedder)` ranks synthesized OKF concept pages the same way `retrieve` ranks raw items, scoring each concept as the better of its centroid similarity and any single member's similarity so a diluted centroid can't hide it. `ask`/`stream_ask` gate concepts through the exact same `threshold` as raw items (abstention only when neither clears it), then recruit each cleared concept's member items fresh from the live `KBEntry` (never the concept's own copied `quote`/`timestamp`) and append its already-grounded `ConceptClaim` prose as extra synthesis context; post-synthesis citation validation still runs against the full recruited-item set, so T-Q2/T-Q3 faithfulness is unchanged. Concepts remain strictly a consumer of item-level retrieval, never a replacement. Phase 16 (concept<->concept edges) and Phase 17 (`concepts merge/split/sync-pending` CLI) remain out of scope. T-Q9-13 added; ruff clean. |

---

## Decisions needed (owner answers here)
| #  | Question                                                            | Default                        | Owner decision |
|----|---------------------------------------------------------------------|--------------------------------|----------------|
| D1 | Score per-document or per-application-link?                         | per-document (per-link optional) | **per-document** |
| D2 | Include YouTube URL fetch in MVP, or transcript text only? *(superseded by Phase 13 — YouTube video/playlist fetch via yt-dlp shipped 2026-08-08)* | text only in MVP               | **MVP: text only; paste OR file upload (.srt/.txt/.md); handle transcripts with or without timestamps** (FR1, FR21, FR22). **Phase 13: YouTube video/playlist URL fetch added** (T-Y*). |
| D3 | Commit generated `kb/` to git, or keep local/gitignored?           | gitignored (git-remote backup) | **gitignored; back up via scheduled push to a separate private repo** |
| D4 | LLM provider/model to default to?                                   | Claude API, model via env      | **Claude API, model via env** |
| D5 | Embeddings: local model or API?                                     | local (provider-independent)   | **local** (watch instance RAM when hosted — ARCH §8.4) |
| D6 | Hosting target?                                                     | Railway (auth required)        | **Railway, auth required** |
| D7 | Auth method when hosted?                                            | built-in single-user secret    | **built-in single-user secret** |

## Changelog (agent appends)
- 2026-06-15 Phase 0 (0.1–0.4) done: scaffold, LLM boundary, pytest harness, CI workflow. 9 unit tests green, ruff clean. Checkpoint held (CI wired on empty-but-working project).
- 2026-06-15 Phase 1 (1.1–1.3) done: models, store, ingest. 39 unit tests green, ruff clean. Checkpoint held (construct/persist/reload KBEntry+Profile; uniform transcript across formats).
- 2026-06-15 Phase 2 (2.1) done: profile_update.py pure EMA logic, all SCHEMA §3 rows proven in isolation. 50 unit tests green. Checkpoint held (feedback→profile rules, no LLM). Also: ruff ignores UP017 to keep `timezone.utc` runnable on the 3.10 dev sandbox (3.11-valid too).
- 2026-06-15 Phase 3 (3.1–3.3) done; 3.4 in review: triage prompt+parse+short-circuit. 57 unit tests green. Eval tests T-T4/T-T5 written and gated but UNRUN (no API key in build env) — owner to run `pytest -m eval` with a key to confirm the checkpoint.
- 2026-06-15 Phase 4 (4.1,4.2) done; 4.3 in review: type-routed extraction + in-code quote discipline + deterministic faithfulness gate. 71 unit tests green. The headline faithfulness EVAL (T-E3) is written/gated/UNRUN — owner to run with a key. Guarantee NOT weakened: normalize (Phase 5) drops any item failing quote_in_transcript.
- 2026-06-15 Phase 5 (5.1) done: normalize.py pure gate (drop unverifiable, backfill provenance, merge dups, preserve stance). 78 unit tests green. Checkpoint held.
- 2026-06-15 Phase 6 (6.1–6.3) done: link.py goal-tied application links, deterministic novelty reservation, cold-start uses stable goals. 84 unit tests green. Checkpoint held.
- 2026-06-15 Phase 7 (7.1–7.2) done: graph.py deterministic topic-overlap candidate lookup + enum-bounded relation classify (capped 3 candidates). 89 unit tests green. Checkpoint held.
- 2026-06-15 Phase 8 (8.1) done: pipeline.py orchestrates 0→6; honors little_to_extract short-circuit; LLM budget bounded. 93 unit tests green. Checkpoint held. Follow-up: derive topic tags (currently empty) to strengthen graph candidacy.
- 2026-06-15 Phase 9 (9.1–9.3) done: Typer CLI run/score/list/show with friendly errors. 100 unit tests green + manual end-to-end smoke (run→list→show→score→profile updated). MVP loop usable from terminal. Checkpoint held.
- 2026-06-15 Phase 10 (read layer) done: Embedder, vector store, reindex, ranked retrieval, ABSTENTION gate (zero-LLM on miss), grounded synthesis w/ source links + conflict surfacing, CLI ask/reindex. 126 unit tests green + e2e ask smoke. Headline T-Q2/T-Q3 proven hermetically. Eval T-Q7 gated/unrun. Checkpoint held.
- 2026-06-15 Phase 11.2/11.3 + 12.1 done: auth gate (fail-closed when public w/o secret, 401 on data routes, localhost open), FastAPI app (list/view/score+ask box), $PORT bind. 135 unit tests green (T-A1..A4 + UI).
- 2026-06-15 Phase 11 complete: Dockerfile (model baked at build, binds 0.0.0.0:$PORT), docker-compose (volume-backed kb/+data/), .dockerignore, scripts/backup_kb.sh (git-remote KB backup), release.yml (test-gated). README verified against all 6 CLI commands. 11.7 tag deferred to owner. BUILD COMPLETE through v0.2 scope; only gated evals + the v0.0.1 tag push remain for the owner.
- 2026-06-15 VERIFICATION: independent subagent review confirmed both headline guarantees HOLD in code (faithfulness drop-gate in normalize.py:29; abstention gate unreachable-synthesis in query.py:111-118; grounding filter query.py:131-145). No bypass paths.
- 2026-06-21 Note v1 done: added DistilledNote + note.py grounded synthesis, bounded extra note call, fallback on malformed output, markdown/web teaching-note rendering, note topics as entry tags, and query retrieval context from cited note sections. 163 unit tests green.
- 2026-06-21 Source metadata UX done: CLI/web accept optional YouTube URLs, uploaded filenames are cleaned before display, Note v1 evidence is collapsed/de-emphasized in markdown and web, and index titles prefer synthesized note titles. 171 unit tests green.
- 2026-06-21 YouTube metadata + deletion done: YouTube URLs fetch best-effort oEmbed metadata (title/channel/channel URL/thumbnail/provider/fetched_at), notes render retained video metadata, and CLI/web deletion removes markdown, index row, and vectors. Unit tests/lint green.
- 2026-08-08 Phase 13.1 done: YouTube video/playlist ingest (OKF Phase 1) — youtube.py fetches captions via yt-dlp and enumerates playlists, web ADD input enqueues one `youtube` job per video through the existing Worker, per-video failures are skipped+reported (never fatal to the batch). `youtube` extra added. 206 unit tests green.
- 2026-08-08 Phase 14 (14.1-14.2) done: per-video OKF export layer (OKF Phase 2) — okf.py derives a neutral `sources/`+`raw/`+index bundle alongside `kb/` at the File stage (transcript-gated, so feedback-only re-files leave it untouched), okf_lint.py validates it (E1-E4, stdlib only). Neutrality (no feedback/application-links) and slug stability verified by dedicated unit tests. concepts/entities/canonicalization/OpenKnowledge wiring remain out of scope for a later phase. 235 unit tests green, ruff clean.
- 2026-08-08 Phase 15.1 done: concept canonicalize matching engine (OKF Phase 3a) — canonicalize.py decides match/new/reject per knowledge item against a `concepts` SQLite table (embedding-similarity + token-overlap candidate pool, one capped batched LLM call per video, graph.py/note.py validation discipline), idempotent via `retract_entry_concept_memberships` (also wired into `delete_entry`). Engine + data model only — no concept pages, OKF export, or pipeline wiring (lands in 15.2/15.3). T-CANON1-7,9 plus model/store unit tests added; 215 unit tests green in this sandbox (pytest tests/unit, excluding the pre-existing `web`/fastapi extras gap unrelated to this change), ruff clean.
- 2026-08-08 Phase 15.2 done: concept-page synthesis + OKF export (OKF Phase 3b) — synthesize_concept.py grounds claims per concept (note.py-style validation, deterministic fallback) with code-rendered citations; okf.export_concept/remove_concept write/retire `okf/concepts/<id>.md` and regenerate indexes; okf.render_source_with_concepts adds the post-canonicalize "## Concepts covered" source backlink; canonicalize.synthesize_touched_concepts caps per-video synthesis (default 5, `DISTIL_CONCEPTS_SYNTH_PER_VIDEO`) via `Store.concept_centroid`, deferring the rest as `pending_synthesis`. Still no pipeline wiring, `enable_canonicalize` flag, or lint checks (15.3). T-SYN1-4, T-OKFC1-4, T-CANON8 added; 281 unit tests green (full `web`/fastapi extras installed), ruff clean.
- 2026-08-08 Phase 15.3 done: canonicalize wired into the pipeline + turned on (OKF Phase 3, closing out) — canonicalize.run_canonicalize_stage is the one Stage 8 orchestration entry point, wired into pipeline.run_pipeline behind `PipelineConfig.enable_canonicalize` (default `True`); okf_lint.py gained E5-E8 for the `concepts/` bundle. §6 validation gate run 3/3 against the real model + a real local embedder with the existing `CONCEPT_SIM_FLOOR`/`MAX_CONCEPT_CANDIDATES` defaults unchanged; gate-produced bundle lints clean, so the default stays `True` per the honest-risk rule. T-PL5-6, T-OKFL5-8, T-CANON-EVAL1-2 added; 287 unit tests green, ruff clean. AGENTS.md rewritten (not appended) per its own maintenance convention to describe the fully-wired state.
- 2026-08-08 Phase 18 (18.1) done: query-over-concepts (OKF Phase 3d) — `query.retrieve_concepts` ranks synthesized concept pages against the question via the same embed-and-cosine method as `retrieve`, blending in per-member similarity so a diluted centroid can't hide a concept; `ask`/`stream_ask` gate concepts through the same `threshold` as raw items (never a lower bar), recruit cleared concepts' members from the live `KBEntry`, and add grounded `ConceptClaim` prose as extra synthesis context, with citation validation still running against the full recruited-item set. Concepts remain a consumer of item-level retrieval, never a replacement. T-Q9-13 added (plus two pre-existing web streaming tests updated to stub the new call); ruff clean. Phase 16/17 remain out of scope.
- 2026-08-09 Phase D (entities layer) done: tools/people/organizations ride along in the existing extraction call as a nested `entities` array per knowledge item (zero new transcript reads, zero new pipeline stages) — a third closed vocabulary (`EntityKind`: tool/person/organization) kept deliberately separate from `type`/`stance` in the prompt; malformed entities are cleaned per-mention in `extract._clean_entity_mentions` before the parent item is ever validated, so one bad entity can never drop its knowledge item. `canonicalize.py` extends Stage 8 (not a new stage) with `canonicalize_entry_entities`/`synthesize_touched_entities`, the exact concept match/new/reject/synthesis-capping shape one granularity down (`Store.find_entity_candidates` hard-filters by `kind` first). `okf.py`/`okf_lint.py` (E9-E12)/`reconcile.py`/`canonicalize.run_delete_entry_stage` all extend the concept treatment to `entities/`; `query.retrieve_entities` gates entities through the identical `threshold` as items/concepts. Web app gained `/entities` + `/entities/{id}`. No backfill: existing entries simply carry no entities, verified by dedicated graceful-degradation tests. 419 unit tests green, ruff clean.
- 2026-08-09 Phase E (playlist up-front fetch + persistent staging) done: a playlist's transcripts are now all fetched before distilling starts, overlapping with the (still single-worker) distill `Worker` instead of interleaving one fetch per distill. `web/jobs.py` gained a second background thread, `Fetcher`, that claims a new `pending_fetch`/`fetching` status pair one video at a time — disjoint from `queued`/`running`, so it never contends with the distill `Worker` — pausing `DISTIL_PLAYLIST_FETCH_DELAY_SECONDS` (default 3.0s) between fetches after the first. A successful fetch stages the parsed transcript as JSON on the persistent volume (`kind=youtube_staged`) and flips the job to `queued`, so the distill Worker treats it exactly like an uploaded file. `recover_interrupted` now also recovers crashed `fetching` jobs back to `pending_fetch`. Fixed a live latent bug in the same pass (had to ship together — playlist staging would have hit the identical failure mode): uploaded-file staging (`_UPLOAD_DIR`) lived under `tempfile.gettempdir()`, wiped on every container restart/redeploy even though the job row survives on the volume; staging (uploads + prefetched transcripts) now derives from `DISTIL_DB_PATH`'s own directory (override via `DISTIL_STAGING_DIR`). Follow-up review fix in the same phase: reading a staged file no longer deletes it, since a downstream pipeline failure right after the read used to leave `retry()` with nothing to re-read; `Worker.on_finished` now reclaims the file only on a successful (done/low_value) outcome, `JobStore.autoclear`'s `on_stale_failed` hook reaps a permanently-failed job's file 24h after the fact, and job removal / `jobs/clear` still reclaim it immediately. T-J1-9 added; 463 unit tests green, ruff clean. `distil/youtube.py` and the extraction/concept/entity paths are untouched.

- 2026-08-09 External-collector queue (server half) done: a bot-check refusal (`distil.youtube.is_bot_check_refusal`, a substring match on yt-dlp's verbatim "Sign in to confirm you're not a bot" line — high confidence in practice, not structural *(this confidence claim turned out wrong the day it was written — see the Phase 24 entry below)*) now parks a job in a third lifecycle, `awaiting_collection` -> `collecting` (leased, expiring) -> `queued`/`youtube_staged`, disjoint from `queued`/`running` and `pending_fetch`/`fetching`; every other fetch failure still fails immediately as before. `web/jobs.py` gained a leased work queue (`claim_for_collection`/`release_expired_leases`/`submit_collected_transcript`/`report_uncollectable`), guarded-UPDATE exclusive claims (proved under real concurrent threads), idempotent submit/report against a lost-response retry, and a 7-day `collection_deadline` (`DISTIL_COLLECTOR_EXPIRY_SECONDS`) swept on the existing `list_active()`/Activity-poll cadence so the queue self-cleans with zero collectors running. `web/app.py` exposes `/collector/jobs/claim|/{id}/transcript|/{id}/unfetchable`, gated by a separate Bearer credential (`DISTIL_COLLECTOR_TOKEN`, `web/auth.py`'s `request_is_collector_authorized`) proved unable to reach owner routes or vice versa; a submitted transcript is validated via `ingest_srt_text` before ever entering the pipeline. Only the server queue — no collector program or waiting-state UI (later pieces). New coverage in `tests/unit/test_web_collector.py`, `tests/unit/test_web_jobs.py`, `tests/unit/test_youtube.py` (T-J10-15, T-A5, T-COL1-3, T-Y17 in `TESTING.md`); full-suite count not reverified in this pass (no Python/pytest available in this environment).

- 2026-08-09 Bot-check detection fix (Phase 24) done: the line above's `is_bot_check_refusal` never actually matched in production — its literal `_BOT_CHECK_MARKER` used a straight `'` apostrophe while YouTube's real refusal text uses a curly `'` (U+2019), confirmed from production failures on videos `AbpyqAfxZ8c`/`QER-0DaC-Gk`; every bot-checked video failed immediately instead of reaching `awaiting_collection`, and the original tests couldn't catch it because their fixture shared the same wrong straight-quote assumption as the implementation. Replaced the literal with `_BOT_CHECK_RE`, a case-insensitive regex tolerant of the apostrophe/quote character (or none at all); added `PotDiagnostic.bot_check_detected` (surfaced by `distil youtube-diagnose-pot` / `GET /diagnostics/youtube-pot`) so the safety net's liveness can be checked directly instead of inferred from silence. Tests rebuilt around real captured stderr (`_REAL_BOT_CHECK_STDERR`) plus a parametrized apostrophe-variant/case-insensitivity sweep. See AGENTS.md's Phase 24 entry and T-Y21/T-Y22 in `TESTING.md`. 580 unit tests green, ruff clean.

- 2026-08-09 Waiting-for-collector UI (Helper 3) done: now that Phase 24 lets bot-checked videos actually reach `awaiting_collection`, the Activity view's status map had no entry for it or `collecting` — a parked video rendered as "Unknown" and dropped out of live polling, indistinguishable from the app being broken. `web/jobs.py`'s `status_presentation`/`collector_status_for_job` (server-computed, unit-testable; `web/templates/index.html` is now a thin renderer of the values `GET /jobs` already attaches) give both states their own badge, keep them in the live-polling set, and explain the wait in owner terms — why it's parked, the `collection_deadline` countdown, and whether a collector is actually checking in (`JobStore.record_collector_checkin`/`last_collector_checkin`, a new `collector_meta` row updated on every authenticated `/collector/jobs/claim`, even an empty one). `remove_queued` now also accepts `AWAITING_COLLECTION` (never `COLLECTING`, which has a lease a collector may be mid-fetch against), and the 7-day expiry failure message now names YouTube and the collector explicitly. Parking rules, bot-check detection, and the collector HTTP protocol are untouched. See AGENTS.md's Helper 3 entry and T-J16-21 in `TESTING.md`. 590 unit tests green, ruff clean.

- 2026-08-09 Trust the owner's decision on what to keep (supersedes 3.3/8.1's short-circuit); triage+extract merged (owner decision) done: `run_pipeline` no longer stops on a `little_to_extract` verdict — the only rejection left is `distil/ingest.py`'s word-count floor (`TranscriptTooShortError`, `DISTIL_MIN_TRANSCRIPT_WORDS`=50), enforced at ingest on all four ingest paths, distinguishable everywhere from a fetch failure and mapped to `STATUS_LOW_VALUE` (never `STATUS_FAILED`). `triage.verdict` is kept for shape compatibility only; a word count can't catch a partial fetch, so `Source.transcript_word_count`/`ingest.is_thin_source` (`DISTIL_THIN_TRANSCRIPT_WORDS`=500) surface thin material as a visible, non-rejecting badge instead. `Store.list_entries()`'s verdict-based pruning (the same bug reintroduced at listing time) was removed. Folded into the same task (avoids a second validation run on the same function): the narrative summary (PR #26) now starts at the front of the pipeline on a daemon thread and runs concurrently with a merged `extract.run_triage_extract` call (`<TRIAGE>` then `<ITEMS>` in one strong-tier response, replacing separate `triage.run_triage`/`extract.run_extraction` calls) — bounded join via `DISTIL_SUMMARY_JOIN_TIMEOUT_SECONDS` (default 60s) right before filing, independent failure reporting in both directions. `triage.py`'s own classifier is kept (not deleted) for the gated eval suite. See AGENTS.md's "Triage no longer gates the pipeline" and "Triage and extraction are merged" entries, and ARCHITECTURE.md §2's updated stage diagram, for the full reasoning. Extraction-quality-vs-split-path comparison and a real ingest cost measurement are written as a gated eval (`tests/eval/test_triage_extract_merge_eval.py`) but UNRUN — no `ANTHROPIC_API_KEY` in the authoring environment.

## Agent notes (non-blocking observations)
- ENV: stack pins Python >=3.11 (ARCHITECTURE.md §1) and CI uses 3.11. The dev sandbox here runs 3.10, so `pip install -e .` is refused by `requires-python`; tests are run via `PYTHONPATH=.` instead. No stack change made — flagging only. If the owner wants the sandbox to do editable installs, lowering the floor to 3.10 would be a stack decision (raise in Decisions needed first).
